export interface ContactFormProps {
  open: boolean;
  onClose: () => void;
}

export interface ExperienceProps {
    jobTitle: string;
    company: string;
    duration: string;
    description: string;
}